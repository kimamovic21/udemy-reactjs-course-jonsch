import React from 'react';

const Footer = ({ children }) => {
    // console.log(children);
    
    return (
        <footer>
            {children}
        </footer>
    );
};

export default Footer;